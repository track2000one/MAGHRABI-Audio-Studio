--extra-index-url https://download.pytorch.org/whl/cpu

# Runtime application dependencies.
-r requirements.txt

# Railway CPU ML stack is pinned to the first PyTorch line that fixes
# CVE-2025-32434 while keeping Torch/TorchAudio on an officially matched pair.
torch==2.6.0+cpu
torchaudio==2.6.0+cpu
